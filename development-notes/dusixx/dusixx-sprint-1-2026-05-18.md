# Спринт 1: Настройка проекта и создание компонентов — 2026-05-18

## Что было сделано:

### 1) Создание и настройка репозитория, регистрация команды

- Создал репозиторий, добавил коллабораторов 
- Настроил правила для веток (`main`, `dev`). 
- Создал проект командой `ng new`. 
- Настроил `path aliases` в файле `tsconfig.app.json`. 
- Добавил все необходимое для комфортной работы: линтер (`eslint`, `stylelint`, `commitlint`), форматтер (`prettier`). Добавил конфиги для перечисленных выше инструментов. 
- Установил `husky` и добавил три скрипта: `commit-msg`, `pre-commit`, `pre-push`. 
- Внес изменения в `angular.json`. 
    - Изменил стандартный префикс `app`. 
    - Добавил `changeDetection: OnPush`, чтобы каждый раз во вновь созданных компонентах не прописывать руками. 
    - Добавил `stylePreprocessorOptions.includePaths`, чтобы при импорте стилей не указывать абсолютный путь. 
- Добавил шаблон для ПР. 
- Добавил скрипты в `package.json` для линтинга, форматирования и инициализации husky. 
- Добавил структуру проекта (папки). Которую также обсудили с ментором. 
- Создал команду в `RS App > Team distribution`. 

Никаких проблем в процессе не возникло. На все ушло порядка 5 часов. В результате по умолчанию стал тимлидом. Роль абсолютно формальная.

### 2) Создание кастомной кнопки

Создал кастомную кнопку с вариантами (variant), разными размерами (size), возможностью добавить svg иконку (icon) и с возможностью отображать спиннер в состоянии `loading`. Ушло порядка 30 часов вместе с рефакторингом вплоть до мержа в `dev`.

### 3) Создание Error page

Создал страницу `404` для несуществующих маршрутов (`path: '**'`). Добавил картинку, подходящую, на мой взгляд, под тематику приложения (`Music Streaming Service`). Ушло порядка 2 часов с поиском и обработкой картинки. Проблем не возникло.

## Проблемы и решения: 

Проблемы возникли при создании кастомной кнопки. Идея была в том, чтобы инкапсулировать в компоненте всю логику и прокидывать необходимые свойства (`input`) без добавления лишней разметки. 

Реализовал первую версию и понял, что в компонент нельзя пробросить нативные для `button` атрибуты/события (click, aria-label, type, title...). Например, в Реакте такая задача является тривиальной. Более того, там можно создать тип объединяющий кастомные и стандартные свойства компонента. 

Полез искать и выяснил, что такой функционал лучше реализовать атрибутной директивой. Тогда кнопка остается собой и есть возможность задать кастомные и нативные свойства. Реализовал вторую версию директивой. Однако, спиннер и иконку приходилось добавлять в разметку вручную:

```html
<button playerButton>
  <player-spinner/>
  <player-sprite href="..."/>
</button>
```
Для этого создал два компонента - `Sprite` и `Spinner`. Чтобы минимизмровать кол-во разметки. 
Добавил `effect` в директиву, в котором для состояния `!loading` запоминал ширину (`offsetWidth`) для кнопки. И в состоянии `loading`, когда отображается только спиннер - инлайн-стилем применял ширину к кнопке. Идея в том, чтобы ширина не "скакала" в разных состояниях. Возникли нюансы. 

Во-первых, сохраненная ширина была неадекватной или даже 0. Ибо к этому моменту, когда вызывался еффект, еще не успевала отрендериться сама кнопка. Обернул код для сохранения в `raf` - помогло, но это скорее костыль. Кнопка все-равно прыгала. Оказалось, что в `offsetWidth` округленное значение. Перешел на `getBoundingClientRect().width`. Стало ок. 

Во-вторых, захотел сбрасывать паддинги у кнопки без текста (`textContent`). Даже когда текст меняется через devtools (избыточно). Сделал это не самым оптимальным образом через `ngAfterViewChecked`. Определял длинну текста и накидывал класс `no-text`. Так как возможности писать код больше не было, сделал ПР этого (сырого) решения. 

Во время ревью Ина указала, что компонент спиннер, вероятно, лишний. А логика директивы не выглядит оптимальной. В процессе рефакторинга перенес логику контроля за текстом кнопки в функцию `afterEveryRender`. В итоге пересчет длинны текста стал вызываться в несколько раз реже. Один раз на рендер приложения, а не для компонента и его потомков. 

Логику сохранения ширины кнопки перенес в `afterRenderEffect`, что позволило избавиться от `raf`. Так же оказалось, что в директиве можно отрендерить и спиннер, и иконку. Использовал для этого `Renderer2`. В конструкторе создаю иконку (`svg > use`) и спиннер внутри кнопки. Добавил свойство `icon`, в которое прокидывается `href` для `use`. Такое решение не универсально, но хорошо работает когда есть спрайт со всеми кионками для приложения. Нужно реализовать возможность задавать иконки не только из спрайта. 

По итогу инкапсулировал всю логику внутри директивы. Текущее решение позволяет задавать кнопку в разметке следующим образом:

```html
<button 
  playerButton 
  variant="secondary" 
  size="small" 
  icon="assets/icons/icons.svg#icon-sun" 
  [loading]="loading()" 
  aria-label="sun">
Sun
</button>
```

## Что узнал: 

Эксперимент с созданием кастомной кнопки показал, что лучше использовать готовые решения (библиотеки компонентов) для чего-то относительно сложного в плане дополнительной логики. Тем не менее было интересно и познавательно. Планирую использовать эту кнопку в приложении. 

Узнал про хуки жизненного цикла в тч про `ngAfterViewChecked `. И как понимаю - это уже легаси. Узнал про реактивные функции: `effect`, `afterRenderEffect`, которые вызываются при изменении сигналов внутри. Узнал про глобальную функцию `afterEveryRender`. Узнал как получить доступ к элементу из компонента и прямо в шаблоне (`ElementRef`,`Template Reference Variable` e.g., `#myButton`)

Узнал как лучше кастомизировать элементы, для которых критична возможность прокидывания нативных свойств (атрибутная директива). Узнал что есть классовая/атрибутная привязка (`[class.small]`, `[disabled]`). Узнал про `ngClass` директиву (в первой реализации кнопки), позволяющую накинуть классы объектом типа `Record<string, boolean>`

## Планы: 

Подробнее познакомиться с жизненным циклом компонента и нюаснами работы Ангуляра. Узнать о `Dependency Injection`, `Route Guards`, `Signals and Change Detection`, `linkedSignal()`, `model()`. Сделать карточку для треков, еще несколько страниц и в целом реализовать все необходимое (минимум) для индивидуального зачета в рамках второго спринта.

## Затраченное время:

40+ часов