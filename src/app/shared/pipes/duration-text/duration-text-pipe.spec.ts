import { DurationTextPipe } from './duration-text-pipe';

describe('DurationTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationTextPipe();
    expect(pipe).toBeTruthy();
  });
});
