import { PluralTextPipe } from './plural-text-pipe';

describe('PluralTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PluralTextPipe();
    expect(pipe).toBeTruthy();
  });
});
