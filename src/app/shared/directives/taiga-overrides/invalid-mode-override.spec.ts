import { TaigaInvalidModeOverride } from './invalid-mode-override';

describe('TuiTextfieldOverride', () => {
  it('should create an instance', () => {
    const directive = new TaigaInvalidModeOverride();
    expect(directive).toBeTruthy();
  });
});
