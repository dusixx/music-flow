import { StopPropagation } from './stop-propagation';

describe('StopPropagation', () => {
  it('should create an instance', () => {
    const directive = new StopPropagation();
    expect(directive).toBeTruthy();
  });
});
