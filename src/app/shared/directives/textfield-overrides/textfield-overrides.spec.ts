import { TextfieldOverrides } from './textfield-overrides';

describe('TuiTextfieldOverride', () => {
  it('should create an instance', () => {
    const directive = new TextfieldOverrides();
    expect(directive).toBeTruthy();
  });
});
