import { CarouselItem } from './carousel-item';

describe('CarouselItem', () => {
  it('should create an instance', () => {
    const directive = new CarouselItem();
    expect(directive).toBeTruthy();
  });
});
